# Renderer-only extensions for the loan submit diagram.
# This file is NOT a codegen input — do not feed it to embabel-codegen.

triggers:
  - name: "Customer submits loan application"
    linkedCommand: SubmitLoanApplication
    actor: customer
