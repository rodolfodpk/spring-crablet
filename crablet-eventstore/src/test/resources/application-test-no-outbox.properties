# Base test configuration without outbox
spring.config.import=optional:application-test.properties

# Disable outbox for core/domain tests
crablet.outbox.enabled=false

# Override any outbox-specific configs to prevent errors
crablet.outbox.lock-strategy=PER_TOPIC_PUBLISHER

